# CLAUDE.md — プロジェクト共通前提

このリポジトリで作業するすべてのエージェント／セッションが最初に読む文書。
**個別の設計判断ではなく、全員が共有すべき前提だけ**をここに書く。

---

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| 仮題 | 『合図（あいず）』 / *Signal*（英題は要再検討。Steam に同名多数） |
| ジャンル | 2人協力必須パズル・アドベンチャー |
| テーマ | **友情** — 選んで結ばれ、時間とともに変質する関係 |
| プレイ人数 | 2人（同一画面ローカル協力） |
| 目標プレイ時間 | **1〜2時間**（一度の集まりで遊びきれる長さ） |
| プラットフォーム | Steam / PC（Windows 優先、Linux は可能なら） |
| ビジュアル | レトロ基調（ピクセルアート、限定パレット） |
| 対応言語 | 日本語・英語 |

詳細は [`docs/GDD.md`](docs/GDD.md) を参照。**GDD がこのプロジェクトの憲法。**

---

## 2. 技術スタック

- **エンジン: Godot 4.x**（GDScript）
  - 採用理由: シーン・リソース・スクリプトがすべてテキスト形式で、
    AI エージェントが直接読み書き・レビュー・Git 衝突解決できる
- **協力プレイ: ローカル同一画面 + Steam Remote Play Together**
  - **ネットワークコードは書かない。** これは技術的制約ではなく設計上の決定
- バージョン管理: Git
- CI: 未定（フェーズ4で `steam-release-manager` が設計）

### ⚠️ Remote Play Together の制約（設計に直結）

RPT は**ホストの画面をそのまま相手に配信する**方式のため:

- ❌ 「相手にだけ見える情報」は成立しない（相方の画面も見えている）
- ❌ 各プレイヤー個別の解像度・UI スケールは持てない
- ✅ 非対称性は「**見えるものの違い**」ではなく「**触れられるものの違い**」で表現する
- ✅ 分割画面は使ってよい（両者が同じ映像を見る前提で設計する）
- ✅ 入力は2つのコントローラ／キーボード分割の両方に対応する
- ⚠️ 音声通話の遅延があるため、シビアな同時押しを要求しない（許容誤差 0.5 秒以上）

新機能を設計するときは、必ずこの制約に照らして検証すること。

**本作は分割画面を採用している**（GDD §4-1）。二人は「同じ時代の、別の場所」にいる。
分割は**空間**であって**時間**ではない。二人を別の時代に固定する設計は、
GDD §4-4「合図」が発生しなくなるため**禁止**。

---

## 3. スコープ境界（最重要）

このプロジェクトの最大のリスクは「機能の作りすぎで完成しないこと」。
背景と根拠は [`docs/03-scope-management.md`](docs/03-scope-management.md)。

### 初リリース（v1.0）に入れないもの

以下は**議論の余地なく v1 対象外**。提案されたら `docs/GDD.md` の「将来検討」に送る。

- 独自オンラインネットコード／マッチメイキング／ロビー
- 3人以上のプレイ、シングルプレイモード
- ボイス／音声収録
- 分岐エンディング、周回要素、メタ進行
- 日本語・英語以外の言語
- レベルエディタ、MOD 対応
- コンソール移植、モバイル対応
- 実績以外の Steam 機能（トレカ、ワークショップ、クラウドセーブ以外の連携）

### 判断ルール

新機能の提案には、必ず次を問う。

1. GDD のコアループに**直接**寄与するか？ しないなら却下
2. 追加コストは「実装時間」ではなく「**既存の何を壊すか**」で見積もる
3. 迷ったら削る。削って失敗しても出せるが、作りすぎた失敗は出せずに終わる

---

## 4. エージェント間の通信規約

サブエージェントは互いに会話できず、毎回ゼロの context で起動する。
**通信路はリポジトリ上のファイルだけ。**

### 書き込み担当の割り当て（重複禁止）

| ディレクトリ | 書き込んでよいエージェント |
|---|---|
| `docs/GDD.md` | **メイン会話のみ**（＝ユーザーとの対話）。エージェントは読むだけ |
| `docs/specs/` | `game-design-reviewer` は読むだけ。仕様確定はメイン会話 |
| `src/`（Godot プロジェクト） | `gameplay-engineer` |
| `tests/`, `docs/qa/` | `qa-engineer` |
| `docs/ui/` | `ux-designer` |
| `docs/art/` | `art-director` |
| `docs/audio/` | `audio-director` |
| `docs/text/` | `narrative-writer` |
| `docs/release/` | `steam-release-manager` |
| `docs/marketing/` | `marketing-researcher` |

### エージェントの作業手順

1. `CLAUDE.md`（本ファイル）と `docs/GDD.md` を読んで前提を把握する
2. 自分の担当ドキュメントの現状を読む
3. 作業する
4. **担当ファイルを更新してから**要約を返す
   （返り値の要約は消えるが、ファイルは残り、次のエージェントが読める）

### 越権の禁止

- 担当外のディレクトリに書き込まない
- 仕様判断が絡むものは**自分で決めず、報告する**
- `docs/GDD.md` は絶対に書き換えない

---

## 5. コーディング規約（Godot / GDScript）

- ファイル名・ディレクトリ名: `snake_case`
- クラス名・シーンファイル名（`.tscn`）: `PascalCase`
- 変数・関数: `snake_case`、定数: `UPPER_SNAKE_CASE`
- privateな要素は `_` 接頭辞
- 型注釈を付ける（`var speed: float = 0.0`）
- ゲーム内に表示される文字列を**コードに直書きしない**。
  必ず `docs/text/` 由来のローカライズキー経由で参照する（英語対応のため）
- マジックナンバーは名前付き定数にする（バランス調整のため）

## 6. ディレクトリ構成

```
/
├─ CLAUDE.md              ← 本ファイル
├─ README.md
├─ docs/                  ← 設計・調査ドキュメント
│   ├─ GDD.md             ← 憲法
│   ├─ specs/             ← 機能別の詳細仕様
│   ├─ art/ audio/ ui/ text/ qa/ release/ marketing/
├─ src/                   ← Godot プロジェクト本体
├─ tests/
└─ .claude/agents/        ← サブエージェント定義
```
